# API extras (on top of the project's training deps: torch, torch-geometric, transformers).
fastapi>=0.110
uvicorn[standard]>=0.29
pydantic>=2.0
pyyaml>=6.0
sqlalchemy>=2.0
psycopg[binary]>=3.1        # PostgreSQL driver (prod). SQLite needs no driver (dev default).
