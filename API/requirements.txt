# This service depends ONLY on the gnn_vuln model library as a published package —
# it never reaches into a source tree outside API/. gnn-vuln is installed from PyPI
# (`pip install gnn-vuln`) and transitively brings torch, torch-geometric, transformers.
# NOTE: torch + torch-scatter/torch-sparse are pre-installed from their own indexes in
# the Dockerfile (matching torch+CUDA wheels), since PyPI alone can't resolve them.
gnn-vuln==0.1.12

# Web service layer
fastapi>=0.110
uvicorn[standard]>=0.29
pydantic>=2.0
pyyaml>=6.0
python-multipart>=0.0.9     # multipart/form-data parsing for POST /datasets/upload
sqlalchemy>=2.0
psycopg[binary]>=3.1        # PostgreSQL driver (prod). SQLite needs no driver (dev default).
boto3>=1.34                 # S3 / MinIO client (object storage for CPG blobs)
celery[redis]>=5.3          # async job queue (long-running ingestion / relearn) + Redis broker
